apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest-api-helmdemo.fullname" . }}
  labels:
    {{- include "rest-api-helmdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rest-api-helmdemo.selectorLabels" . | nindent 4 }}

